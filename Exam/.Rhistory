tmp = c(1L, 2.5, 3L)
is.integer(tmp)
any(tmp > 0)
any(tmp < 0)
all(tmp > 0)
all(tmp <0)
tmp = c(0L, 1L,2L)
any(tmp < 0)
tmp = c(0L, 1L,2L, -1L)
any(tmp < 0)
source("~/Documents/GitHub/AppliedStatisticalProgramming2022/Exam/easyPois/R/PoisMLE.R")
is.character("abc")
# ------------------------------------------------------
# Load libraries and set working directory
# ------------------------------------------------------
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
document(current.code, quiet = TRUE) # Make the help files
3L < 0
3L > 0.0
lfactorial(10)
factorial(10)
?lfactorial
lfactorial(10)
log(lfactorial(10))
factorial(10)
tmp
tmp = c(1L, 3L, 5L)
factorial(tmp)
log(factorial(tmp))
# ------------------------------------------------------
# Load libraries and set working directory
# ------------------------------------------------------
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
document(current.code, quiet = TRUE) # Make the help files
replicate(B, {return(sample(y, n, replace = TRUE))})
replicate(10, {return(sample(y, n, replace = TRUE))})
replicate(10, {return(sample(tmp, n, replace = TRUE))})
replicate(10, {return(sample(tmp, 3, replace = TRUE))})
replicate(10, {(sample(tmp, 3, replace = TRUE))})
replicate(10, {(sample(tmp, 3, replace = TRUE))})
tmp = c(1:10)
tmp
replicate(10, {(sample(tmp, length(tmp), replace = TRUE))})
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
document(current.code, quiet = TRUE) # Make the help files
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE)
# Load all of the functions so you can use them
load_all(current.code, quiet = F)
# ------------------------------------------------------
# Load libraries and set working directory
# ------------------------------------------------------
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = F)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = F)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = F)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = F)
# ------------------------------------------------------
# Load libraries and set working directory
# ------------------------------------------------------
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = F)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code)
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code)
document(current.code) # Make the help files
easyPois::estimatePois
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code)
document(current.code) # Make the help files
1L:10L
y <- c(1L:10L)
y
easyPois::mle(y)
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings
document(current.code, quiet = TRUE) # Make the help files
estimatePois(c(50, 70, 80, 99, 82, 43), "basic", 223)
estimatePois(c(50, 70, 80, 99, 82, 43), "basic", B = 100, lambda = 2)
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- 1L:20L
estimatePois(y, "basic", B = 100, lambda = 2)
estimatePois(y, "bootstrap", B = 100, lambda = 2)
estimatePois(y, "basic", lambda = 2)
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- 1L:20L
estimatePois(y, "basic", lambda = 2)
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- 1L:20L
estimatePois(y, "basic", lambda = 2)
estimatePois(y, "bootstrap", B = 1000, lambda = 2)
estimatePois(y, "basic", lambda = 2)
estimatePois(y, "bootstrap", B = 100000, lambda = 2)
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings
document(current.code, quiet = TRUE) # Make the help files
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings
document(current.code, quiet = TRUE) # Make the help files
current.code <- as.package("easyPois")
# Load all of the functions so you can use them
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings
document(current.code, quiet = TRUE) # Make the help files
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- 1L:20L
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- 1L:20L
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# Set working directory
# ------------------------------------------------------
# setwd("/Users/ysui/Documents/GitHub/AppliedStatisticalProgramming2022/Exam")
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
y <- sample(1L:20L, 100, replace = TRUE)
estimatePois(y, "basic", lambda = 2)
plot(estimatePois(y, "bootstrap", B = 1000, lambda = 2))
logLik(y, lambda = 2)
estimatePois(y, "basic", lambda = 2)
n <- length(y)
(-n * lambda) - sum(log(gmp::factorialZ(y))) + log(lambda) * sum(y)
lambda <- 2
(-n * lambda) - sum(log(gmp::factorialZ(y))) + log(lambda) * sum(y)
(-length(y) * lambda) - sum(log(factorial(y))) + (log(lambda) * sum(y))
-n*lambda - sum(log(exp(lfactorial(y)))) + log(lambda)*sum(y)
-n*lambda - sum(log(exp(lfactorial(y)))) + log(lambda)*sum(y) # Rex
logLik(y, lambda = 2)
mle(y)
sum(y, na.rm = TRUE) / n
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
set.seed(1023)
y <- sample(1L:20L, 100, replace = TRUE)
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
logLik(y, lambda = 2)
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
set.seed(1023)
y <- sample(1L:20L, 100, replace = TRUE)
n <- length(y)
lambda <- 2
estimatePois(y, "basic", lambda = 2)
estimatePois(y, "bootstrap", B = 1000, lambda = 2)
logLik(y, lambda = 2)
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
set.seed(1023)
y <- sample(1L:20L, 100, replace = TRUE)
n <- length(y)
lambda <- 2
estimatePois(y, "basic", lambda = 2)
estimatePois(y, "bootstrap", B = 1000, lambda = 2)
logLik(y, lambda = 2)
mle(y)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
document(current.code, quiet = TRUE) # Make the help files
# ------------------------------------------------------
# Test functions
# ------------------------------------------------------
set.seed(1023)
y <- sample(1L:20L, 100, replace = TRUE)
n <- length(y)
lambda <- 2
estimatePois(y, "basic", lambda = 2)
estimatePois(y, "bootstrap", B = 1000, lambda = 2)
logLik(y, lambda = 2)
mle(y)
standardError(y, "bootstrap", B = 1000)
standardError(y, "bootstrap", B = 1000)
library(devtools)
library(roxygen2)
# ------------------------------------------------------
# load package easyPois
# ------------------------------------------------------
current.code <- as.package("easyPois")
# Load all of the functions
load_all(current.code, quiet = TRUE) # please don't tell me all the warnings :)
# Make the help files
document(current.code, quiet = TRUE)
# ------------------------------------------------------
# Testing some functions
# ------------------------------------------------------
set.seed(1023)
y <- sample(1L:20L, 100, replace = TRUE)
n <- length(y)
lambda <- 2
estimatePois(y, "basic", lambda = 2)
estimatePois(y, "bootstrap", B = 1000, lambda = 2)
logLik(y, lambda = 2)
mle(y)
standardError(y, "bootstrap", B = 1000)
